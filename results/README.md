# Результаты

В этой папке хранятся экспортированные таблицы и графики с итогами экспериментов.

```text
results/
├── tables/
└── figures/
```

В `tables/` можно хранить компактные CSV-таблицы с финальными метриками, а в `figures/` — графики для README и текста ВКР.
